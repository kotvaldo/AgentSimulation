package entities;

import Statistics.TimeWeightedStatistic;
import simulation.MySimulation;

public class QueueAssembly extends Queue{
    public QueueAssembly(MySimulation mySimulation) {
        super(mySimulation);
    }
}
