package entities;

import Statistics.TimeWeightedStatistic;
import simulation.MySimulation;

public class QueueNonProcessed extends Queue{
    public QueueNonProcessed(MySimulation mySimulation) {
        super(mySimulation);
    }
}
