package entities;

import Statistics.TimeWeightedStatistic;
import simulation.MySimulation;

public class QueuePainting extends Queue {
    public QueuePainting(MySimulation mySimulation) {
        super(mySimulation);
    }
}
