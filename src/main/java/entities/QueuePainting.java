package entities;

import Statistics.TimeWeightedStatistic;

public class QueuePainting extends Queue {
    public QueuePainting(TimeWeightedStatistic queueLength) {
        super(queueLength);
    }
}
